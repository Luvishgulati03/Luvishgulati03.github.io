# Review screenshots — regenerated by scripts/audit.mjs on every run. Tracking them made the
# tree dirty after each audit, which blocked the nightly stats workflow (it refuses to push a
# dirty tree). Nothing on the site links to them; they are local review artifacts only.
shots/

.DS_Store
